// Cleaner-facing dashboard. The shared page component branches on role and
// renders the cleaner view here (the admin view lives at /admin/dashboard).
export { default } from "@/app/admin/dashboard/page";
export const dynamic = "force-dynamic";
